# DATA_SOURCE: "json" (default, no DB needed) or "postgres"
DATA_SOURCE=json

# Only used when DATA_SOURCE=postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/punha_bharari

# Optional overrides
COMPANIES_JSON_PATH=data/companies_seed.json
DEFAULT_TOP_N=10
